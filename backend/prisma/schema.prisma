// Este arquivo define a configuração do Prisma e a fonte de dados.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Modelos serão definidos nas próximas etapas do projeto.